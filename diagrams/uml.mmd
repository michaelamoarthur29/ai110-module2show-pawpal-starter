classDiagram
    %% PawPal+ — matches the implementation in pawpal_system.py

    class Owner {
        +str name
        +list~Pet~ pets
        +add_pet(pet Pet) Pet
        +get_pets() list~Pet~
        +get_all_tasks() list~Task~
    }

    class Pet {
        +str name
        +str species
        +int age
        +list~Task~ tasks
        +add_task(task Task) Task
        +get_tasks() list~Task~
        +has_task(title str, date_time datetime) bool
    }

    %% Task.priority: "high" | "medium" | "low"
    %% Task.frequency: "none" | "daily" | "weekly"
    class Task {
        +str title
        +str task_type
        +datetime date_time
        +int duration_minutes
        +str priority
        +bool completed
        +str frequency
        +mark_complete() None
        +end_time() datetime
        +is_recurring() bool
        +overlaps(other Task) bool
        +priority_rank() int
    }

    class Scheduler {
        +Owner owner
        +pets() list~Pet~
        +get_all_tasks() list~Task~
        +pet_for(task Task) Pet
        +get_sorted_tasks() list~Task~
        +get_daily_tasks(day date) list~Task~
        +detect_conflicts(day date) list~tuple~
        +create_recurring_tasks(until date) list~Task~
    }

    Owner "1" *-- "0..*" Pet : owns
    Pet "1" *-- "0..*" Task : has
    Scheduler "1" --> "1" Owner : reads tasks through
